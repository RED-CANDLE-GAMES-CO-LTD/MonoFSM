- 新增 AnimatorStateNameSource / AnimatorStateNameEntry：依 condition 順序 override AnimatorPlayAction 的 state name；抽出 IStateHashProvider 取代原本對 AnimatorStateStringListProvider 的特判。
- 2026-09-15 RigidbodyRootMotionCustomReceiver 改成「root motion delta → rb.linearVelocity / angularVelocity」，不再 MovePosition / MoveRotation。原因：(1) kinematic rb 上同一 tick 兩次 MovePosition 後者贏，飛行怪 chase clip 只要 root 有一點旋轉曲線就把 SimpleFlyingCharacter 的位移蓋掉、原地不動；(2) dynamic rb 上 MovePosition 是瞬移，位移不進 velocity，NetworkRigidbody 同步不到，proxy 外推會一格一格修。另外加兩道閘門：MonoObj.HasStateAuthority（MonoObj.AfterSimulate 本身不擋 proxy，proxy 端畫面交給 NRB 插值）與 `_isAnimationDriven` VarBoolWrapper（該綁 SimpleChController 同一顆 VarBool，兩邊互斥；不綁時預設 true 沿用舊行為）。失敗理由走 ApplyResult enum 顯示在 Inspector。已知近似：OnAnimatorMove 是 render frame、AfterSimulate 是 tick，pending/tick dt 與 CharacterMotionReceiver 同款；要決定性就走 RootMotionSimulateClipMoveAction。
